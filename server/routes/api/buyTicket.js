// const express = require('express');
// const router = express.Router()
// const {buyTicket} = rewuire('../controllers/buyTicket')


// router.get('/', buyTicket) 

// router.getById('/:id', (req, res) => { 
//     resstatus(200).json({message: 'Show only one ticket '})
// })

// router.post('/:id', (req, res) => { 
//     resstatus(200).json({message: 'Display a ticket '})
// })

// router.put('/:id', (req, res) => { 
//    resstatus(200).json({message: 'Show only one root '})
// })
// router.delete('/:id', (req, res) => { 
//     resstatus(200).json({message: 'Remove ticket and refund  the money '})
//  })

// module.export = router
